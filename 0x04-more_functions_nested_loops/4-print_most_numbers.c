#include "main.h"

/**
 * print_numbers - prints numbers 0-9
 */
void print_numbers(void)
{
	char  c;

	for (c = '0'; ch <= '9'; c++)
	{
		if (!(ch == '2' || ch == '4'))

		_putchar(c);
	}

	_putchar('\n');
}
