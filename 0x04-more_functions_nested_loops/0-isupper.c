#include "main.h"
#include <stdio.h>
#include <ctype.g>
/**
 * _isupper - chceks if character is upper case
 * @c: character to test
 * Return: 1 if uppercase otherwise 0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
 }
