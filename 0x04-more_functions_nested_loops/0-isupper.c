#include "main.h"
#include <stdio.h>

/**
 * _isupper - chceks if character is upper case
 * @c: character to test
 * Return: 1 if uppercase otherwise 0
 */
int _isupper(int c)
{
	char c;
	c == 'A';
	{
		return (1);
	}
	c == 'a';
	{
		return (0);
	}
}
