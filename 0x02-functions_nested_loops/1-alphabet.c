#include "main.h"

/**
 * main- prints in stdout
 *
 * Return: Always 0.
 */

void print_alphabet(void);
int main(void)
{
	char letter = 'a';
	while (letter <= 'z')
	{
	_putchar(letter);
	letter++;
	}
_putchar('\n)
return (0);
}
