#include "main.h"
/**
 * main - prints outputin stdout
 *
 * Return: Always 0.
 */
void print_alphabet(void);

int main(void)
{
	char letter = 'a';

	while
	(letter <= 'z')

	{
	_putchar(letter);
	letter++;
	}
_putchar('\n');
print_alphabet();
return (0);
}
