#include "main.h"
/**
 * main- check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void);

int main(main)
{
char letter = 'a';
while
(letter <= 'z')

for (int i = 0; i < 10; i++)
{
_putchar(letter, i + 1)
letter++;
}
_putchar('\n');
return (0);
}
