#include "main.h"
#inlude <stdio.h>

/**
 * is upper - checks for uppercase
 * Return 1 if lower case otherwise  0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

