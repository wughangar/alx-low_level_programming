#include <stdio.h>
/**
 * main- prints the sentence in stdout
 *
 * Return: Always 0 (success)
 */
int main(void)
{
char letter = 'a';
char letter2 = 'A'
while (letter <= 'z')
{
putchar (letter);
letter++;
}
while (letter2 <= 'Z')
{
putchar (letter2);
letter2++
}
putchar('\n');
return (0);
}
