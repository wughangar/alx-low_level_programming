#include "main.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

int _putchar(char c)
{
	/* TODO: implements the function */
	return (0);
}

int _islower(int c)
{
	/* TODO: implements the function */
        return (0);
}

int _isalpha(int c)
{
	/* TODO: implements the function */
        return (0);
}

int _abs(int n)
{
	/* TODO: implements the function */
        return (0);
}

int _isupper(int c)
{
	/* TODO: implements the function */
        return (0);
}
