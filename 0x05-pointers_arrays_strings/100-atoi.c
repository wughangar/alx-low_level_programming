#include "main.h"
#include <stdlib.h>

/**
 * _atoi - convert a string to an integer
 * @s: the string to convert
 * Return: the integer contained within the string
 */
int _atoi(char *s)
{
	return (atoi(s));
}
