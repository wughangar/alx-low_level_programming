#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * rev_string- function to reverse string
 * @s: string to be reversed
 * Return: 0.
 */
void rev_string(char *s)
{
	char s;
	{
		_putchar(strrev(s));
	}
	return (o);
}
