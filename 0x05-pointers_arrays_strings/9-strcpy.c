#include "main.h"
#include <string.h>

/**
 * _strcpy - copies the specified string to the destination string
 * @dest: the string to copy to
 * @src: the source string
 * Return: the destination string
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
